This is a express server serves a static react app, as well as an API for the react app to use

to run it, first run `npm run build` in the /client directory

then run `npm start` in the /server directory
